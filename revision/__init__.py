# Revision experiments for TDSC-2025-11-2261 (major revision).
# Self-contained package addressing reviewer requests:
#   - ablation isolating the VQC (R1-2, R2-3)
#   - dense noise sweep with multiple seeds (R2-1)
#   - noise-aware training comparison (R2-2)
#   - VQC sensitivity over qubits/layers/ansatz (R2-4)
#   - dataset bias audit via QR decoding (R1-1)
# Perturbations act on UN-normalized [0,1] images (pre-normalization noise model).
